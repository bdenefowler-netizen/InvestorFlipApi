{
  "$schema": "https://railway.com/railway.schema.json",
  "deploy": {
    "startCommand": "cd backend && python -m importers.daily_cron --limit 2000",
    "cronSchedule": "0 11 * * *",
    "restartPolicyType": "NEVER"
  }
}
