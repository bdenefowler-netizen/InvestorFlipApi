{
  "$schema": "https://railway.com/railway.schema.json",
  "deploy": {
    "startCommand": "cd backend && python -m importers.tax_roll_sync --apply",
    "cronSchedule": "*/5 * * * *",
    "restartPolicyType": "NEVER"
  }
}
