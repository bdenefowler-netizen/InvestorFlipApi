{
  "$schema": "https://railway.com/railway.schema.json",
  "deploy": {
    "startCommand": "python patch_upload_runtime.py && env CORS_ALLOWED_ORIGINS=* uvicorn server:app --host 0.0.0.0 --port $PORT"
  }
}
